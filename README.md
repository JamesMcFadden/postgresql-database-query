# postgresql-database-query
This repository implements a system for querying a postgresql database with a docker setup.

To start docker, run the followig command from root:
docker-compose up -d

To run:
python -m launchlog.cli [ARGS]